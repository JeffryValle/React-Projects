import { createContext } from "react";


// Paso 1: creamos el contexto
// Paso 2: proveemos el contexto a toda la app
// Paso 3: utilizamos el contexto a conveniencia
export const Contexto = createContext();
